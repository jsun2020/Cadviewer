# Scoped re-review of the fix wave (959fdca..60d32c2)

The dispatched re-review agent went idle three times without producing a
deliverable. The controller performed the new-breakage analysis directly,
with real data from the sample drawing. Verdict below is the controller's.

## Finding verdicts

All ten code findings were verified as ADDRESSED by (a) the fix-wave report's
per-finding test table, (b) the full suite passing at 146 tests (up from 110,
i.e. 36 new regression tests), and (c) clippy clean. I4 and I7 were spec/plan
decisions per controller ruling and are recorded.

Direct evidence for the two Criticals:
- C1: new tests absurd_arc_angles_terminate_instead_of_looping_forever,
  an_arc_with_an_overflowing_angle_is_dropped_not_hung,
  overflowing_and_infinite_reals_are_clamped_to_zero. Suite green.
- C2: new tests two_coincident_candidates_leave_exactly_one_survivor,
  a_nearly_coincident_duplicate_leaves_the_larger_one. Real drawing still
  detects 26 sheets.

## New-breakage check (the important half) — measured, not argued

Q1 Does the lexer's non-finite clamp corrupt legitimate large coordinates?
   NO. On the real drawing: $EXTMIN (-927500.5, -2047265.7), $EXTMAX
   (957715.5, -161406.9), model_extents 1,377,450 x 1,885,859 units. Values
   near +/-2 million survive intact. A clamp bug would have zeroed these.

Q2/Q3 read_blocks -> BlockRecord{base,entities}: all consumers updated?
   YES. sheets::detect still returns 26 sheets in the correct row shape.

Q4 Expansion budget silently truncating an ordinary drawing?
   NO. report.truncated == false on every sheet built.

Q5 Layer off/frozen/plottable skipping hiding geometry that should plot?
   NO wholesale suppression. 191 layers, exactly 1 with negative ACI.
   Per-sheet "entities on non-plotting layers" skipped: 242-256, a modest
   and plausible number.

Q6 Does the new clip remove visible content?
   NO. Pages 1 and 3 render with full frame, title block, axis grids and
   content inside the printable area.

Q7 Anomaly investigated: sheet 3 builds 1,334 items vs ~256,000 for sheets
   1-2 (190x gap). NOT a bug. Rendered page 3: it is the base building plan
   (green axis grid, walls, stairs, few fixtures), genuinely far sparser than
   the equipment-dense HVAC sheets. Verified visually.

## Residual, deferred (not blocking)

- Skipped-kind counts in BuildReport are accumulated over the whole document
  traversal rather than per page, so identical totals appear on every sheet
  (HATCH 6796, TEXT 4132, MTEXT 3500, ATTRIB 2886, ATTDEF 1557). Cosmetic
  reporting issue; the counts are correct globally, misleading per page.
- Perf unchanged and worse after the fix wave: 26-page export 9m29s (was
  5m45s) because DIMENSION/MINSERT/heavy-POLYLINE now actually draw.
  Diagnosed (I7), deferred to the Phase 6 spatial-index work.

## Verdict: ALL ADDRESSED, no new Critical or Important breakage.
